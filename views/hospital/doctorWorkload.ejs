<!DOCTYPE html>
<html lang="en" class="h-full bg-slate-50 antialiased">
<head>
  <meta charset="UTF-8">
  <title>Doctor Workload | Smart Health</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: { sans: ['Inter', 'sans-serif'] },
          colors: {
            brand: { 50: '#ecfdf5', 100: '#d1fae5', 500: '#10b981', 600: '#059669', 700: '#047857' }
          }
        }
      }
    }
  </script>
</head>

<body class="min-h-full">

<nav class="bg-white border-b border-slate-200 sticky top-0 z-30">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      
      <div class="flex items-center gap-3">
        <div class="h-8 w-8 bg-brand-100 rounded-lg flex items-center justify-center">
             <span class="text-brand-600 font-bold text-lg">W</span>
        </div>
        <span class="font-bold text-slate-900 text-lg tracking-tight truncate max-w-[200px] sm:max-w-md">
          <%= hospital.hospitalName %> <span class="text-slate-400 font-normal">/ Analytics</span>
        </span>
      </div>

      <div class="flex items-center space-x-3">
        <a href="/hospital/dashboard" class="inline-flex items-center px-3 py-1.5 border border-slate-300 shadow-sm text-xs font-medium rounded-md text-slate-700 bg-white hover:bg-slate-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-brand-500 transition-all">
          <svg class="h-4 w-4 mr-1.5 text-slate-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
          </svg>
          Back to Dashboard
        </a>
      </div>

    </div>
  </div>
</nav>

<main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

  <div class="mb-6">
    <h1 class="text-2xl font-bold text-slate-900">Workload Distribution</h1>
    <p class="mt-1 text-sm text-slate-500">
      Real-time breakdown of patient volume across OPD and IPD departments per doctor.
    </p>
  </div>

  <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden">
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-slate-200 text-sm">
        <thead class="bg-slate-50">
          <tr>
            <th scope="col" class="px-6 py-3 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider w-12">#</th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Doctor Name</th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Specialization</th>
            <th scope="col" class="px-6 py-3 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider">OPD</th>
            <th scope="col" class="px-6 py-3 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider">IPD (Admitted)</th>
            <th scope="col" class="px-6 py-3 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider">IPD (Discharged)</th>
            <th scope="col" class="px-6 py-3 text-right text-xs font-bold text-brand-700 uppercase tracking-wider bg-brand-50/50">Total</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-200 bg-white">
          
          <% if (workload.length === 0) { %>
            <tr>
              <td colspan="7" class="px-6 py-12 text-center text-slate-500">
                <div class="flex flex-col items-center justify-center">
                   <div class="h-10 w-10 bg-slate-100 rounded-full flex items-center justify-center mb-3">
                     <svg class="h-6 w-6 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-2m3 2v-4m3 4v-6m2 10H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path></svg>
                   </div>
                   <p class="font-medium">No workload data available</p>
                </div>
              </td>
            </tr>
          <% } %>

          <% workload.forEach((w, index) => { %>
            <tr class="hover:bg-slate-50 transition-colors group">
              <td class="px-6 py-4 whitespace-nowrap text-slate-400 font-mono text-xs">
                <%= index + 1 %>
              </td>
              
              <td class="px-6 py-4 whitespace-nowrap font-medium text-slate-900">
                <%= w.doctor.name %>
              </td>
              
              <td class="px-6 py-4 whitespace-nowrap">
                <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-slate-100 text-slate-800 border border-slate-200">
                  <%= w.doctor.specialization %>
                </span>
              </td>
              
              <td class="px-6 py-4 whitespace-nowrap text-right font-mono text-slate-600">
                <%= w.opdCount %>
              </td>
              
              <td class="px-6 py-4 whitespace-nowrap text-right font-mono text-slate-600">
                <%= w.ipdAdmitted %>
              </td>
              
              <td class="px-6 py-4 whitespace-nowrap text-right font-mono text-slate-400">
                <%= w.ipdDischarged %>
              </td>
              
              <td class="px-6 py-4 whitespace-nowrap text-right bg-brand-50/30 group-hover:bg-brand-50/60 transition-colors">
                <span class="font-bold font-mono text-brand-700 text-base">
                  <%= w.total %>
                </span>
              </td>
            </tr>
          <% }) %>

        </tbody>
      </table>
    </div>
  </div>

</main>

</body>
</html>